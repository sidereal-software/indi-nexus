The selected member of a switch vector is now unmistakable. It was drawn with the same
`accent` token the toggles hover to, so a hovered unselected member looked identical to the
selected one, and the fill itself was nearly invisible against the card. The selected
member now wears the same teal as the Set button and carries a tick, so it does not rest on
colour alone - which matters because a switch's selected member is the instrument's state,
not decoration.
