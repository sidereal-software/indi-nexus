INDINexus is published to PyPI as `indi-nexus` and to npm as `@indi-nexus/client` and
`@indi-nexus/react`. Releases are cut from `main` by release-please and uploaded with
trusted publishing, so no registry credentials are stored in the repository.
