# Changelog

Notable changes to INDINexus: the `indi-nexus` Python package and the
`@indi-nexus/client` and `@indi-nexus/react` npm packages, which release in lockstep and
so share one changelog.

This file is generated by [towncrier](https://towncrier.readthedocs.io/) from the entries
in `changelog.d/`. Do not edit it by hand - add an entry to `changelog.d/` instead, as
described in [DEVELOPMENT.md](DEVELOPMENT.md).

The documentation site renders everything below this line, pulled in from here with a
pymdownx snippet, so the release notes live in exactly one place.

<!-- --8<-- [start:releases] -->

<!-- towncrier release notes start -->

## 0.1.0 - 2026-08-10


### Python package (indi-nexus)

#### Fixed

- `indi-nexus serve` now starts even when `indiserver` is unreachable, instead of hanging
  in startup with no output. The panel loads and reports a disconnected state, and the
  bridge connects on its own once `indiserver` appears. `IndiClient.start()` gained a `wait`
  keyword for this; its default is unchanged, so scripts and monitors still block until the
  first connection.

#### Packaging

- Published to PyPI as `indi-nexus`, so `pip install indi-nexus` gets the driver SDK, the
  async client, the web bridge and the bundled reference panel. Releases are cut from `main`
  by release-please and uploaded with trusted publishing, so no registry credentials are
  stored in the repository.
- The source distribution no longer bundles `web/node_modules` (about 8 MB of build
  dependencies), and it now contains the TypeScript sources of `@indi-nexus/client` and
  `@indi-nexus/react`, which were being dropped from it.


### Frontend (@indi-nexus/client, @indi-nexus/react)

#### Packaging

- Published to npm as `@indi-nexus/client` and `@indi-nexus/react`, released in lockstep with
  the Python package and uploaded with trusted publishing.

#### Documentation

- `@indi-nexus/client` and `@indi-nexus/react` now ship a README and a copy of the licence,
  so both have documentation on their npm pages.
