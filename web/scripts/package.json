{
  "name": "@indikit/doc-snippets",
  "version": "0.0.0",
  "private": true,
  "description": "Extracts the documentation's TypeScript fences into modules the packages typecheck.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
